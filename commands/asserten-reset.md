---
description: Clear the current asserten session (start fresh on a different agent).
---

Run:

```bash
"${CLAUDE_PLUGIN_ROOT}/bin/asserten-cli" reset
```

Confirm to the user that the session is cleared.
