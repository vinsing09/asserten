---
description: Show the current asserten session — what's been ingested, evaluated, optimized.
---

Run the asserten status command and show the user the result:

```bash
python -m client.cli status
```

Show the output as-is.
