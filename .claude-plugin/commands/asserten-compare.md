---
description: 4-way comparison table — v0 vs v1 vs v2a vs v2b pass_rates and deltas.
---

# /asserten-compare

```bash
python -m client.cli compare
```

Shows a markdown table with pass_rates and deltas vs v0 for all four versions. Empty cells mean that version hasn't been eval'd yet.

This is the demo punchline — show it as the final result of a full asserten run.
